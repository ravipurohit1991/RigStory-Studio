Apply the following correction to the motion plan as one `MotionPlanPatch`
JSON object.

Correction request:
{{instruction}}

Selection (only these actions should change):
{{selection}}

Current motion plan:
{{plan_json}}

Return a JSON object that conforms to this JSON schema:
{{schema}}
