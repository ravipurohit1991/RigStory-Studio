# ADR 0006: Maximum Of Two Actors

Status: Accepted

## Context

The product scope and motion planning constraints are explicitly capped at two character actors per scene.

## Decision

RigStory Studio will enforce a maximum of two actors in API, domain validation, and UI when scene features are introduced.

## Consequences

Increasing the actor limit requires architectural review and a new ADR.
