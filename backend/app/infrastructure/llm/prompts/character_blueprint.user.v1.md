Create one `CharacterBlueprint` for the following request.

Free-text description:
{{description}}

Form choices already selected by the user (preserve these exactly):
{{form_summary}}

Return a JSON object that conforms to this JSON schema:
{{schema}}
