Plan the following scenario as one `MotionPlan` JSON object.

Scenario:
{{prompt}}

Scene snapshot (the only ids you may reference):
{{snapshot}}

Actor rig capabilities:
{{rig_summary}}

Supported action types:
{{action_catalog}}

Return a JSON object that conforms to this JSON schema:
{{schema}}
